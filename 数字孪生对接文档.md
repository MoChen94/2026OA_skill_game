# OA ↔ 数字孪生平台 对接文档

> 版本：v1.2（2026-09）
> 适用：数字孪生平台开发/对接人员
> OA 侧联系人：OA 系统管理员
> 网络环境：局域网（HTTP），OA 服务地址以实际部署为准，如 `http://192.168.1.100:8001`

**变更记录**
- v1.2：新增「能力边界速览」章节（已开放能力 / 未开放模块 / 操作边界说明）
- v1.1：取消 API Key 鉴权（白名单免鉴权）；下发接口移除 alarmType / planFinishTime 字段；新增指派工程师（assigneeUsername）、自定义优先级与工单类型；新增报修单列表、工程师列表、设备列表接口
- v1.0：初版

---

## 一、对接总览

数字孪生平台的「工单管理」与 OA 通过以下方式协同：

| 能力 | 方向 | 方式 | 接口 |
|---|---|---|---|
| 调度员在孪生平台下发工单（可指派工程师） | 孪生 → OA | 孪生调用 OA 接口 | `POST /api/v1/work-orders/external` |
| 孪生工单管理同步 OA 工单列表（全量，同调度员视角） | 孪生 → OA | 孪生定时拉取（支持增量） | `GET /api/v1/twin/orders` |
| 孪生查看工单详情/流转进度 | 孪生 → OA | 按需拉取 | `GET /api/v1/twin/orders/{id}` |
| 跟踪报修单审核进展（孪生下发的 P2~P4） | 孪生 → OA | 按需/定时拉取 | `GET /api/v1/twin/repair-requests` |
| 下发时选择工程师（下拉框数据） | 孪生 → OA | 按需拉取 | `GET /api/v1/twin/engineers` |
| 下发时选择设备（下拉框数据） | 孪生 → OA | 按需拉取 | `GET /api/v1/twin/devices` |
| 工单状态变更实时通知 | OA → 孪生 | OA 主动回调（需配置回调地址） | `POST {回调地址}` |
| 设备实时状态推送 | 孪生 → OA | 孪生推送 | `POST /api/v1/devices/sync-status` |

**推荐同步策略**：
1. **列表同步**：孪生每 10~30 秒调用 `GET /twin/orders?updated_after=上次同步时间` 增量拉取；
2. **准实时通知**：OA 侧配置回调地址后，工单每次状态变更（派单/接单/完成/验收/取消）会立即 POST 通知孪生，孪生收到后按需调详情接口刷新；
3. 两者可并用：回调保证实时性，定时增量拉取兜底防丢。

### 能力边界速览

**已开放（孪生端可直接调用）**：

| 类别 | 能力 |
|---|---|
| 指派工程师 | 工程师列表查询；下发工单时指定工程师直接派单 |
| 关联设备 | 设备台账全量查询；下发工单时按设备编号关联 |
| 工单数据 | 全系统工单列表（同 OA 调度员视角）、工单详情、完整流转日志、增量同步 |
| 报修单数据 | 孪生下发的 P2~P4 单据审核进展，立项后关联正式工单号 |
| 工单下发 | 标题/描述/优先级/工单类型/指派工程师，与 OA 网页新建工单能力对等 |
| 设备状态 | 孪生向 OA 推送设备实时状态（联动台账与大屏） |
| 状态通知 | 工单每次状态变更 OA 实时回调孪生 |

**未开放（OA 内部协同模块）**：公告通知、审批中心、保养计划、报表统计、文件共享、用户/部门管理。孪生端如需其中某类数据，联系 OA 管理员评估增加。

**操作边界**：孪生端对工单为「**下发 + 只读**」——工单派发后的接单、完成、验收等流转动作由 OA 侧（工程师/调度员）操作，孪生端不下发流转指令。如业务上需要孪生端也能取消工单或改派工程师，联系 OA 管理员扩展。

---

## 二、鉴权方式

**免鉴权（局域网白名单模式）**：所有孪生 → OA 的接口**无需任何请求头、无需登录、无需密钥**，局域网内直接调用即可。

- 兼容说明：带 `X-API-Key` 请求头也不影响（会被忽略），旧调用方式无需修改；
- 安全边界：本系统部署于公司内网，OA 服务的可访问范围即对接白名单。

---

## 三、接口详情（孪生 → OA，均免鉴权直接调用）

### 3.1 下发工单（孪生侧调度员操作）

```
POST /api/v1/work-orders/external
```

**请求体**（JSON），字段与 OA 网页"新建工单"对齐：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| deviceCode | string | 是 | 设备编号，须已存在于 OA 设备台账（如 EQ-001） |
| alarmLevel | int | 否 | 告警等级 1~4，默认 3（1 最紧急）。用于未指派时的分级处理与默认优先级映射 |
| title | string | 否 | 工单标题，默认自动生成 |
| description | string | 否 | 工单描述，默认自动生成 |
| location | string | 否 | 位置（并入自动生成的描述） |
| sensorSnapshot | object | 否 | 传感器数据快照（原样存档，工单详情可查） |
| assigneeUsername | string | 否 | **指派工程师账号**（如 engineer3，可用 `GET /twin/engineers` 拉取列表做下拉框）。指定后直接生成工单并派单，视为已审核 |
| priority | string | 否 | 优先级 P1~P4，不传按 alarmLevel 映射（1→P1 ... 4→P4） |
| orderType | string | 否 | 工单类型 FAULT 故障维修 / INSPECTION 巡检 / MAINTENANCE 保养 / TASK 任务，默认 FAULT |

**处理规则**：

| 场景 | 结果 |
|---|---|
| 带 `assigneeUsername`（任意等级） | **直接生成工单并派单**给该工程师（状态=待接单），等同 OA 调度员新建时指定工程师 |
| 不带指派 + `alarmLevel = 1`（P1 紧急） | 生成紧急工单；设备台账有负责人则自动派单，否则待 OA 调度员派发 |
| 不带指派 + `alarmLevel = 2~4` | 生成**报修单**，由 OA 调度员审核立项后才成为正式工单（审核进展用 3.4 接口跟踪） |

指派的账号须为 OA 中启用状态的工程师账号，否则返回 400。

**请求示例**：

```json
{
  "deviceCode": "EQ-004",
  "alarmLevel": 3,
  "title": "输送线皮带打滑",
  "description": "皮带出现打滑，边缘磨损，请求检修调整",
  "assigneeUsername": "engineer2",
  "priority": "P2",
  "orderType": "FAULT"
}
```

**响应示例**：

```json
{
  "kind": "work_order",
  "data": {
    "id": 42, "order_no": "WO20260906-001", "title": "输送线皮带打滑",
    "order_type": "FAULT", "order_type_label": "故障维修",
    "priority": "P2", "priority_label": "高",
    "status": "PENDING_ACCEPT", "status_label": "待接单",
    "device_code": "EQ-004", "device_name": "输送线 CV-04",
    "assignee_name": "技术员2", "creator_name": "系统",
    "created_at": "2026-09-06T10:30", "updated_at": "2026-09-06T10:30:00"
  }
}
```

`kind` 为 `work_order`（直接成单）或 `repair_request`（转报修，`data.request_no` 为报修单号）。

### 3.2 拉取工单列表（同步到孪生工单管理）

```
GET /api/v1/twin/orders
```

不带过滤条件时返回**全系统所有工单**（与 OA 调度员视角一致）。

**查询参数**：

| 参数 | 类型 | 说明 |
|---|---|---|
| status | string | 可选。PENDING_DISPATCH / PENDING_ACCEPT / PROCESSING / PENDING_VERIFY / COMPLETED / CANCELLED |
| device_code | string | 可选。按设备编号过滤 |
| updated_after | datetime | 可选。**增量同步**：只返回该时间之后有变更的工单，如 `2026-09-06T10:00:00` |
| page / page_size | int | 分页，默认 1 / 20，page_size 最大 200 |

**响应示例**：

```json
{
  "total": 25, "page": 1, "page_size": 20,
  "items": [
    {
      "id": 42, "order_no": "WO20260906-001", "title": "[数控机床 CNC-01] 异常告警",
      "order_type": "FAULT", "order_type_label": "故障维修",
      "priority": "P1", "priority_label": "紧急",
      "status": "PROCESSING", "status_label": "处理中",
      "device_id": 1, "device_code": "EQ-001", "device_name": "数控机床 CNC-01", "device_location": "一号车间-1F",
      "assignee_name": "技术员3", "creator_name": "系统",
      "dispatch_time": "2026-09-06T10:35", "accept_time": "2026-09-06T10:40",
      "finish_time": null, "verify_time": null, "cancel_time": null,
      "created_at": "2026-09-06T10:30",
      "updated_at": "2026-09-06T10:40:00"
    }
  ],
  "server_time": "2026-09-06T10:45:00"
}
```

> 增量同步建议：记录本次结果中的最大 `updated_at`（或响应 `server_time`），下次请求作为 `updated_after` 传入。

### 3.3 工单详情 + 流转日志

```
GET /api/v1/twin/orders/{order_id}
```

**响应**：`{"order": {...同 3.2 工单结构...}, "logs": [{"action": "DISPATCH", "action_label": "派单", "operator_name": "王调度", "detail": "孪生端指派 技术员3", "created_at": "..."}]}`

### 3.4 报修单列表（跟踪审核进展）

```
GET /api/v1/twin/repair-requests
```

孪生下发 P2~P4 告警（未指派工程师）会转成**报修单**等待 OA 调度员审核，审核通过后才成为正式工单。孪生端用此接口跟踪自己下发单据的审核进展。

**查询参数**：`status`（PENDING_REVIEW 待审核 / APPROVED 已立项 / REJECTED 已驳回 / WITHDRAWN 已撤回）+ 分页。

**响应 items 字段**：`request_no` 报修单号、`title`、`priority`、`status` / `status_label`、`reviewer_name` 审核人、`review_comment` 审核意见、`work_order_id` / `work_order_no`（**APPROVED 后指向生成的正式工单号**，可再调 3.3 接口查详情）、`created_at`。

### 3.5 工程师 / 设备列表（下发表单下拉框）

```
GET /api/v1/twin/engineers    # 可指派的工程师：id / username / real_name / title
GET /api/v1/twin/devices      # OA 设备台账：id / code / name / location / status / owner_name
```

### 3.6 推送设备实时状态

```
POST /api/v1/devices/sync-status
```

**请求体**：`{"deviceCode": "EQ-001", "status": "ALARM"}`，status 取值：`RUNNING` 运行 / `ALARM` 告警 / `MAINTENANCE` 维修中 / `OFFLINE` 停机。

---

## 四、工单状态变更回调（OA → 孪生）

OA 管理员在「孪生对接」页面配置回调地址后，工单每次流转（含孪生下发的工单）都会实时 POST：

```
POST {孪生回调地址}
Content-Type: application/json
```

**回调数据**：

```json
{
  "event": "dispatched",
  "orderId": 42,
  "orderNo": "WO20260906-001",
  "title": "输送线皮带打滑",
  "deviceCode": "EQ-004",
  "priority": "P2",
  "status": "PENDING_ACCEPT",
  "assignee": "技术员2",
  "acted_at": "2026-09-06T10:35:00"
}
```

| event | 含义 |
|---|---|
| created | 工单创建（待派发） |
| dispatched | 已派单 |
| accepted | 工程师已接单 |
| completed | 已提交完成（待验收） |
| verified | 验收通过（已完成） |
| verify_rejected | 验收驳回（退回处理） |
| cancelled | 工单取消 |

**孪生侧要求**：收到回调返回 HTTP 200 即可（OA 超时 3 秒，失败会记录在 OA 对接日志中，不重试——请以定时增量拉取兜底）。

**连通性测试**：OA「孪生对接」页面有「测试回调连通性」按钮，会发送 `{"event": "ping", ...}`，孪生侧可直接返回 200。

---

## 五、OA 侧管理页面说明

OA 系统「孪生对接」模块（管理员/调度员可见）提供：

- **对接状态**：回调地址、鉴权方式、孪生最近调用时间、最近成功回调时间；
- **配置修改**：回调地址保存后即时生效（无需重启服务）；
- **连通性测试**：一键验证回调地址可达；
- **对接日志**：最近 50 条交互记录（方向/动作/详情/成败）。

---

## 六、错误码与约定

| HTTP 码 | 场景 | 处理建议 |
|---|---|---|
| 404 | 设备编号未登记 / 工单不存在 | 先调 `GET /twin/devices` 确认设备已在 OA 台账登记 |
| 422 | 参数格式错误 | 检查字段类型（alarmLevel 须为 1~4 整数） |
| 400 | 指派的工程师不存在或不可用 | 用 `GET /twin/engineers` 里的账号 |

**其它约定**：
- 所有时间均为服务器本地时间，ISO 8601 格式；
- 工单在 OA 内的流转（派单/验收等）仍由 OA 调度员操作，孪生侧只读 + 下发；
- 接口文档交互调试：浏览器打开 `http://OA服务器:8001/docs`（Swagger）。

---

## 七、联调步骤建议

1. OA 管理员提供服务地址（如 `http://192.168.1.100:8001`），接口免鉴权直接调用；
2. 孪生侧调 `GET /twin/orders` 验证数据结构；
3. 孪生侧调 `POST /work-orders/external` 下发一张测试工单（可带 assigneeUsername 指派），OA 页面确认收到；
4. OA 配置孪生回调地址，孪生侧验证能收到 `dispatched` 等事件；
5. 孪生工单管理接入定时增量拉取（`updated_after`），完成列表同步；
6. 双方清理测试数据，上线。
