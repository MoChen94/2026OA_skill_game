# OA协同办公平台 — 部署文档

> 适用于：把项目从开发机拷贝到新电脑（服务器、评委机、团队成员电脑）部署运行。
> 核心优势：**除 Python 3.11 外零安装**——数据库用 Python 自带 SQLite，前端依赖已内置在项目里，支持**完全离线部署**（比赛现场无网也能装）。

---

## 一、系统要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10 / 11（64 位） |
| Python | **3.11（64 位）**，必须勾选 "Add python.exe to PATH" |
| 硬盘空间 | 约 200 MB |
| 网络 | 可选。有 `wheels/` 目录时全程离线；无网环境需提前准备好 wheels |

> Python 下载：https://www.python.org/downloads/release/python-3110/
> 安装时勾选 **Add python.exe to PATH**，然后一路下一步。
> 若 `python` 命令提示"未找到"，用 `py -3` 代替，或到 Python 安装目录手动创建 `python.exe` 的副本（Windows 商店的"应用执行别名"可能占用了 python 命令名，去 设置 → 应用 → 高级应用设置 → 应用执行别名 里关掉即可）。

---

## 二、项目结构与拷贝清单

```
OAPlatform/
├── backend/            ✅ 拷贝   后端代码（FastAPI）
├── frontend/           ✅ 拷贝   前端页面（Vue/Element Plus/ECharts 已内置，离线可用）
├── notifier/           ✅ 拷贝   工程师端"桌面提醒助手"
├── tests/              ✅ 拷贝   自动化测试
├── wheels/             ✅ 拷贝   离线依赖包（约 15 MB，联网部署可不拷）
├── requirements.txt    ✅ 拷贝   运行依赖清单
├── requirements-dev.txt✅ 拷贝   测试依赖清单（pytest/httpx）
├── start.bat           ✅ 拷贝   一键启动
├── stop.bat            ✅ 拷贝   一键关闭
├── 安装环境.bat         ✅ 拷贝   一键装环境（venv + 依赖，离线优先）
├── data/oatool.db      ⚠️ 可选   数据库。拷贝=保留现有数据；不拷=首次启动自动生成演示数据
├── data/uploads/       ⚠️ 可选   文件共享与工单附件的实体文件，迁移时建议连目录一起拷
└── .venv/              ❌ 不拷！ 虚拟环境与本机路径绑定，每台电脑用"安装环境.bat"重新创建
```

**最简拷贝方式**：整个文件夹拷走，删掉 `.venv` 目录即可（`__pycache__` 可一并删除）。

---

## 三、新电脑部署步骤（三步）

### 第 1 步：安装 Python 3.11（每台新电脑只需一次）

python.org 下载安装，勾选 "Add python.exe to PATH"。

### 第 2 步：获取项目 + 安装环境

获取项目两种方式任选：
- **方式 A（git 克隆，推荐）**：`git clone https://github.com/MoChen94/2026OA_skill_game.git`（含全部代码、文档、离线依赖包和桌面助手 exe）
- **方式 B（文件夹拷贝）**：把项目文件夹整个拷到新电脑（如 `D:\OAPlatform`），删掉 `.venv` 目录

然后双击 **`安装环境.bat`**：
   - 自动创建虚拟环境 `.venv`（已有则跳过）
   - 自动安装依赖：**优先用本地 `wheels/` 离线装**；没有 wheels 目录才走网络（清华镜像）
   - 看到 "环境安装完成" 即可

### 第 3 步：启动

双击 **`start.bat`** → 自动后台启动服务（任务栏最小化窗口）并打开浏览器。

- 本机访问：`http://127.0.0.1:8001`
- 接口文档：`http://127.0.0.1:8001/docs`（数字孪生对接看这里）
- 演示账号：`admin` / `dispatcher` / `engineer1~4`，密码均为 `123456`
- 关闭：双击 **`stop.bat`**（或关掉任务栏的 OA Server 最小化窗口）

---

## 四、局域网访问配置

其他电脑访问 `http://服务器IP:8001`。若打不开，按顺序检查：

**1. 查服务器 IP**

```
ipconfig            → 找到 IPv4 地址，如 192.168.1.100
```

**2. 放行防火墙端口 8001**（以管理员身份运行 cmd）：

```
netsh advfirewall firewall add rule name="OA 8001" dir=in action=allow protocol=TCP localport=8001
```

首次启动服务时 Windows 弹的防火墙提示，点"允许访问"效果相同。

**3. 确认同一局域网**，双方能互相 ping 通。

> 注意：本项目固定使用 **8001 端口**（8000 已被数字孪生服务占用）。若新机器 8001 也被占用，改 `start.bat` 里的 `--port 8001` 为其他端口，并在局域网访问时用新端口。

---

## 五、数字孪生对接（给孪生组）

**完整接口说明见根目录《数字孪生对接文档.md》**（鉴权方式、参数示例、回调格式、联调步骤齐全）。

- 鉴权：**免鉴权**（局域网白名单模式），直接调用即可
- 核心接口：`POST /api/v1/work-orders/external` 下发工单（可指派工程师）、`GET /api/v1/twin/orders` 同步工单列表（支持增量）、`POST /api/v1/devices/sync-status` 推送设备状态
- 回调地址：OA 系统内「孪生对接」页面配置（保存即生效，无需重启），工单状态变更实时通知孪生
- 环境变量 `OATOOL_SECRET`（JWT 密钥）与 `OATOOL_DB_URL`（数据库路径）可在 `backend/app/config.py` 查看/覆盖

---

## 六、数据迁移、备份与恢复

- **迁移**：拷贝 `data/oatool.db` 一个文件即可带走全部数据（账号、工单、公告等）；文件共享/工单附件的实体文件在 `data/uploads/`，需要的话连目录一起拷。
- **自动备份**：服务每天 03:00 自动备份到 `data/backups/`，保留最近 7 份；管理员可调用 `POST /api/v1/system/backup-now` 手动备份。
- **恢复**：停服务 → 用备份文件覆盖 `data/oatool.db` → 启动。
- **重置为全新演示数据**：停服务 → 删除 `data/oatool.db` → 启动（自动重建并生成演示数据）。

---

## 七、工程师端"桌面提醒助手"部署（免安装）

每台工程师电脑（不跑服务、不需要源码、不需要 Python）都能收到工单提醒：

**方式一（推荐，零安装）**：电脑浏览器打开 `http://服务器IP:8001` 登录页，点击登录页的「下载桌面提醒助手」下载 exe（约 10MB）。双击运行，首次输入服务器地址和账号密码（之后双击即用），窗口最小化即可。

提醒规则（v12，按账号+权限自动切换）：
- **工程师账号**：派给我的新工单弹窗
- **管理员/调度员账号、及被授予「工单跟踪」权限的账号**：与自己相关（我派发/我报修立项/派给我）的工单，在工程师接单、反馈工单（提交验收）、验收通过/驳回、取消时弹窗，标题带工程师姓名
- 所有弹窗**点击可直达该工单详情**（自动打开浏览器）
- 免输入配置：网页右上角铃铛 →「生成免输入配置」下载 ini 放到 exe 同目录，双击即用；换账号/服务器重新生成覆盖即可（`exe --reset` 可清除）

**方式二（源码运行）**：把 `notifier/` 文件夹拷到工程师电脑（需装 Python 3.11），双击 `启动提醒助手.bat`。

> 为什么网页本身弹不出通知？浏览器规定：网页系统通知只在 HTTPS 或本机 127.0.0.1 下可用，
> 局域网通过 `http://IP` 访问时被禁止，所以用独立助手程序实现（自绘置顶弹窗，不依赖系统通知设置）。
> 重新打包 exe：`pip install pyinstaller` 后执行
> `pyinstaller --onefile --name OATool-Notifier notifier\工单提醒助手.py`，
> 再把 `dist\OATool-Notifier.exe` 复制到 `frontend\downloads\`。

---

## 八、运行自动化测试（可选）

```
.venv\Scripts\python.exe -m pytest tests -v
```

测试使用临时数据库，不影响正式数据。答辩时可说明"核心链路有 12 项自动化测试覆盖"。

---

## 九、常见问题

| 现象 | 处理 |
|---|---|
| `python` 命令不存在 | 装 Python 时没勾 PATH；用 `py -3` 代替，或关掉 Windows 商店的"应用执行别名" |
| 双击 start.bat 提示端口被占 | 已有实例在跑（会直接开浏览器）；或 8001 被其他程序占用，换端口 |
| 局域网打不开 | 防火墙没放行 8001（见第四节）；确认同一网段 |
| 服务窗口一关服务就没了 | 正常设计——服务在最小化窗口里运行，请用 stop.bat 或别关窗口 |
| 依赖安装慢/失败 | 确认 `wheels/` 目录随项目拷来（离线装）；联网装用清华镜像 |
| 浏览器界面不更新 | 老版本浏览器缓存，强制刷新（Ctrl+F5） |
| 桌面助手不弹窗 | ① 窗口第一行必须是 v12；② 显示"工单跟踪已开启：正在跟踪 N 张"才有跟踪对象；③ 只提醒启动之后的变化 |
| 下载的助手还是旧版 | 浏览器 Ctrl+F5 强刷下载页（下载已禁缓存）；或命令行 `curl -O http://服务器IP:8001/downloads/OATool-Notifier.exe` |

---

## 十、常用命令速查（不用 bat 时）

```bash
# 安装环境
python -m venv .venv
.venv\Scripts\python.exe -m pip install --no-index --find-links wheels -r requirements.txt -r requirements-dev.txt

# 启动（前台，窗口即服务）
.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8001 --app-dir backend

# 关闭：Ctrl+C 或运行 stop.bat
```
